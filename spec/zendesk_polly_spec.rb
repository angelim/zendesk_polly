require 'spec_helper'

describe ZendeskPolly do
  it 'has a version number' do
    expect(ZendeskPolly::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
